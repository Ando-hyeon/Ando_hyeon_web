import * as _ from "./style";

interface PropsType {}

export function CityReviewModal({}: PropsType) {
  return (
    <_.Container>
      <_.Header></_.Header>
      <_.Body></_.Body>
    </_.Container>
  );
}
